{% extends "base.html.twig" %}

{% block title %}Nouvelle annonce
{% endblock %}

{% block body %}
	<div class="container">
		<div class="form-group">
			{{form_start(form)}}
			{{form_widget(form)}}
			<button type="submit" class="btn btn-primary">Nouvelle annonce</button>
			{{form_end(form)}}
		</div>
	</div>
{% endblock %}
